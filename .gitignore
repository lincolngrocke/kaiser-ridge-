# Reference spreadsheets & documents kept in the project folder — not part
# of the app, so don't track them or show them as untracked noise.
*.xlsx
*.xlsm
*.docx
*.pdf

# Editor / OS cruft
.DS_Store

# Editor backups. legal-brief.md.bak sat in a PUBLIC repo until 2026-09-22;
# the file it backed up was already tracked and newer, so it was pure exposure.
*.bak
*~
